# ETH Spread Monitor

Система мониторинга спредов между срочными и бессрочными фьючерсами на ETH на бирже ByBit с уведомлениями в Telegram и веб-интерфейсом для отображения данных в реальном времени.

## Описание

Приложение постоянно мониторит спред между ценами срочных фьючерсов ETH и бессрочным фьючерсом на бирже ByBit, сравнивает этот спред с Funding Rate (текущим и средним за исторический период) и отправляет сигналы в Telegram бот или отображает на веб-странице, когда спред меньше чем Funding Rate на заданную величину.

### Основные функции

- 📊 Мониторинг цен срочных и бессрочных фьючерсов ETH в реальном времени
- 💰 Отслеживание текущего и среднего Funding Rate
- 📈 Расчет спредов между фьючерсами
- 🚨 Автоматические сигналы в Telegram при достижении пороговых значений
- 🌐 Веб-интерфейс с отображением данных в реальном времени через WebSocket
- ⚙️ Настраиваемые параметры мониторинга

## Архитектура

Проект состоит из следующих компонентов:

### Модули

- **`src/bybit_client.py`** - Клиент для работы с ByBit API (получение цен, Funding Rate)
- **`src/spread_calculator.py`** - Расчет спредов и сравнение с Funding Rate
- **`src/telegram_notifier.py`** - Отправка уведомлений в Telegram
- **`src/monitor.py`** - Основной модуль мониторинга
- **`src/web_server.py`** - Веб-сервер с WebSocket для real-time обновлений
- **`config.py`** - Конфигурация приложения
- **`main.py`** - Точка входа приложения

### Технологии

- **Python 3.8+**
- **FastAPI** - Веб-фреймворк
- **WebSocket** - Real-time обновления данных
- **pybit** - Библиотека для работы с ByBit API
- **python-telegram-bot** - Telegram бот API
- **Pandas/NumPy** - Обработка данных

## Установка

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий** (если еще не сделали):
```bash
git clone https://github.com/dolgoale/ETH_Spread.git
cd ETH_Spread
```

2. **Создайте виртуальное окружение**:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`** на основе примера:
```bash
cp .env.example .env
# Или создайте файл .env вручную
```

5. **Настройте конфигурацию** в файле `.env` (см. раздел Конфигурация)

## Конфигурация

Создайте файл `.env` в корне проекта со следующими параметрами:

```env
# ByBit API Configuration
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here
BYBIT_TESTNET=false

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Monitoring Configuration
SPREAD_THRESHOLD_PERCENT=0.5
FUNDING_RATE_HISTORY_DAYS=7
MONITORING_INTERVAL_SECONDS=5

# Web Server Configuration
WEB_SERVER_HOST=0.0.0.0
WEB_SERVER_PORT=8000

# Symbol Configuration
PERPETUAL_SYMBOL=ETHUSDT
FUTURES_SYMBOLS=ETHUSDT_240329,ETHUSDT_240628,ETHUSDT_240927
```

### Параметры конфигурации

#### Статические параметры (только в .env)
Эти параметры задаются только при запуске через `.env` файл:

#### ByBit API
- `BYBIT_API_KEY` - Ваш API ключ ByBit (можно получить в настройках аккаунта)
- `BYBIT_API_SECRET` - Ваш API секрет ByBit
- `BYBIT_TESTNET` - Использовать тестовую сеть (`true`/`false`)

#### Telegram Bot
- `TELEGRAM_BOT_TOKEN` - Токен бота Telegram (получите у @BotFather)
- `TELEGRAM_CHAT_ID` - ID чата для отправки уведомлений (можно узнать у @userinfobot)


#### Веб-сервер
- `WEB_SERVER_HOST` - Хост для веб-сервера (по умолчанию 0.0.0.0)
- `WEB_SERVER_PORT` - Порт для веб-сервера (по умолчанию 8000)

#### Динамические параметры (можно изменять через веб-интерфейс)
Эти параметры можно изменять через веб-интерфейс и они сохраняются в `config.json`:

- `SPREAD_THRESHOLD_PERCENT` - Порог в процентах для отправки сигнала (по умолчанию 0.5%)
- `FUNDING_RATE_HISTORY_DAYS` - Количество дней истории для расчета среднего Funding Rate (по умолчанию 7)
- `MONITORING_INTERVAL_SECONDS` - Интервал обновления данных в секундах (по умолчанию 5)
- `PERPETUAL_SYMBOL` - Символ бессрочного фьючерса (по умолчанию ETHUSDT)
- `FUTURES_SYMBOLS` - Список символов срочных фьючерсов через запятую

**Примечание**: Изменения динамических параметров через веб-интерфейс сохраняются в файл `config.json` и применяются немедленно без перезапуска приложения.

## Использование

### Запуск приложения

```bash
python main.py
```

Приложение запустится и начнет мониторинг. Вы увидите:

- Логи работы в консоли
- Веб-интерфейс доступен по адресу: http://localhost:8000
- Уведомления будут отправляться в Telegram (если настроено)

### Веб-интерфейс

Откройте браузер и перейдите по адресу: **http://localhost:8000**

Интерфейс состоит из двух вкладок:

#### Вкладка "Мониторинг"
- Текущий Funding Rate
- Средний Funding Rate за период
- Спреды по всем отслеживаемым фьючерсам
- Данные обновляются в реальном времени через WebSocket

#### Вкладка "Настройки"
Позволяет изменять параметры мониторинга без перезапуска приложения:
- **Порог спреда (%)** - порог для отправки сигнала
- **Дни истории Funding Rate** - количество дней для расчета среднего
- **Интервал мониторинга (секунды)** - частота обновления данных
- **Символ бессрочного фьючерса** - символ бессрочного фьючерса (например, ETHUSDT)
- **Символы срочных фьючерсов** - список символов через запятую

Изменения сохраняются в файл `config.json` и применяются немедленно без перезапуска.

### API Endpoints

- `GET /` - Главная страница с веб-интерфейсом
- `GET /api/data` - Получить текущие данные в формате JSON
- `GET /api/config` - Получить текущую конфигурацию (изменяемые параметры)
- `PUT /api/config` - Обновить конфигурацию
- `WS /ws` - WebSocket для получения обновлений в реальном времени

### Остановка приложения

Нажмите `Ctrl+C` для корректной остановки приложения.

## Логика работы

1. **Получение данных**: Приложение запрашивает цены фьючерсов и Funding Rate с ByBit API каждые N секунд (задается в конфигурации)

2. **Расчет спредов**: Для каждого срочного фьючерса рассчитывается спред относительно бессрочного фьючерса:
   - Спред = Цена срочного фьючерса - Цена бессрочного фьючерса
   - Спред % = (Спред / Цена бессрочного фьючерса) × 100

3. **Сравнение с Funding Rate**: 
   - Рассчитывается средний Funding Rate за указанный период
   - Проверяется условие: `Спред % < (Funding Rate % - Порог %)`
   - Если условие выполняется, отправляется сигнал

4. **Уведомления**: 
   - Сигналы отправляются в Telegram (с защитой от спама - минимум 1 час между сигналами для одного фьючерса)
   - Данные обновляются на веб-странице в реальном времени

## Структура проекта

```
ETH_Spread/
├── src/
│   ├── __init__.py
│   ├── bybit_client.py       # Клиент ByBit API
│   ├── spread_calculator.py  # Расчет спредов
│   ├── telegram_notifier.py  # Telegram уведомления
│   ├── monitor.py            # Мониторинг
│   └── web_server.py         # Веб-сервер
├── config.py                 # Конфигурация (с поддержкой динамического изменения)
├── main.py                   # Точка входа
├── requirements.txt          # Зависимости
├── .env.example             # Пример конфигурации (статические параметры)
├── config.json              # Динамическая конфигурация (создается автоматически)
├── .gitignore               # Git ignore
├── README.md                # Документация
└── ARCHITECTURE.md          # Описание архитектуры
```

## Получение API ключей

### ByBit API

1. Зайдите на https://www.bybit.com/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в API Management: https://www.bybit.com/app/user/api-management
4. Создайте новый API ключ
5. Скопируйте API Key и API Secret

**Важно**: Для чтения данных достаточно прав на чтение (Read-only)

### Telegram Bot

1. Найдите бота @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Скопируйте токен бота
5. Для получения Chat ID найдите @userinfobot и отправьте ему сообщение

## Логи

Логи приложения сохраняются в файл `eth_spread_monitor.log` и выводятся в консоль.

## Разработка

### Добавление новых функций

Проект структурирован для легкого расширения:

- Для добавления новой биржи - создайте новый клиент по аналогии с `ByBitClient`
- Для добавления новых каналов уведомлений - создайте новый модуль уведомлений
- Для изменения логики мониторинга - редактируйте `SpreadMonitor`

### Запуск в режиме разработки

```bash
# Установка в режиме разработки
pip install -r requirements.txt

# Запуск с автоперезагрузкой (если нужно)
# Можно использовать uvicorn напрямую для веб-сервера
```

## Устранение неполадок

### Проблемы с подключением к ByBit API

- Проверьте правильность API ключей
- Убедитесь, что API ключ имеет права на чтение данных
- Проверьте интернет-соединение

### Проблемы с Telegram уведомлениями

- Проверьте правильность токена бота
- Убедитесь, что Chat ID указан корректно
- Попробуйте отправить сообщение боту вручную

### Веб-интерфейс не обновляется

- Проверьте, что WebSocket соединение установлено (индикатор статуса на странице)
- Откройте консоль браузера (F12) для просмотра ошибок
- Проверьте, что порт 8000 не занят другим приложением

## Лицензия

[Укажите лицензию]

## Автор

Создано для мониторинга спредов на ETH фьючерсах

## Поддержка

При возникновении проблем создайте issue в репозитории GitHub.
